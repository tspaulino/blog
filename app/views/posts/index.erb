<% @posts.each do |post| %>
    <article>
      <%= link_to(content_tag(:h2, post.title), post_path(post))  %>
      <%= content_tag(:span, "Posted by #{post.author.name} at #{post.updated_at.to_s}") %>
      <%= content_tag(:p, post.teaser) %>
    </article>
<% end %>